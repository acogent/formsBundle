{# {% form_theme form 'SGNTwigCrudBundle:Form:fieldsArray.html.twig' %} #}

<h2>Edit</h2>
{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <input type="submit" />
{{ form_end(form) }}

